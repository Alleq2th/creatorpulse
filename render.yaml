services:
  - type: web
    name: creatorpulse
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: HF_API_KEY
        sync: false
      - key: NEWS_API_KEY
        sync: false
